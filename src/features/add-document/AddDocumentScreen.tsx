import { PlaceholderScreen } from '@/shared/components/PlaceholderScreen';

export function AddDocumentScreen() {
  return (
    <PlaceholderScreen
      title="Dodaj dokument"
      message="Dodawanie dokumentów pojawi się w kolejnych etapach."
    />
  );
}
