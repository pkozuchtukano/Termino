import { PlaceholderScreen } from '@/shared/components/PlaceholderScreen';

export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Ustawienia"
      message="Konfiguracja pojawi się w kolejnych etapach."
    />
  );
}
